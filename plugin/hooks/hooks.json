{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const i=JSON.parse(process.env.TOOL_INPUT||'{}');const c=(i.command||'').toLowerCase();const bad=/rm\\s+-rf|curl.*\\|.*sh|npm.*install.*-g|sudo|chmod|rmdir\\s+\\/s|del\\s+\\/[fq]|format\\s+[a-z]:/.test(c);if(bad){console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',permissionDecision:'deny',permissionDecisionReason:'This command is not needed for Roblox game development. Let us focus on building your game!'}}))}else{console.log('{}')}\""
          }
        ]
      }
    ]
  }
}
